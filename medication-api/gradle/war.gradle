apply plugin: "war"

bootWar {
    mainClassName = "com.dispenser.medication.MedicationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
